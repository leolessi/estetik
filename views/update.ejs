<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Atualizar Consulta</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>

  <body class="bg-light">
    <div
      class="container d-flex justify-content-center align-items-center mt-5 p-0"
    >
      <div class="card shadow-lg p-4 w-100" style="max-width: 500px">
        <!-- <h2 class="text-center mb-4">ATUALIZAR</h2> -->
        <form action="/update" method="post">
          <input type="hidden" name="id" value="<%= appo._id %>" />
          <div class="mb-3">
            <label for="name" class="form-label">Nome</label>
            <input
              type="text"
              class="form-control"
              id="name"
              name="name"
              value="<%= appo.name %>"
              required
            />
          </div>
          <div class="mb-3">
            <label for="serviceType" class="form-label">Tipo de Serviço</label>
            <select class="form-select" id="serviceType" name="serviceType" required>
              <option value="" disabled selected>Tipo de serviço</option>
              <option value="Avaliacao" <%= appo.serviceType === "Avaliacao" ? "selected" : "" %>>
                Avaliação
              </option>
              <option value="Horario Simples" <%= appo.serviceType === "Horario Simples" ? "selected" : "" %>>
                Horário Simples
              </option>
              <option value="Horario Duplo" <%= appo.serviceType === "Horario Duplo" ? "selected" : "" %>>
                Horário Duplo
              </option>
            </select>
          </div>
          <div class="mb-3">
            <label for="name" class="form-label">Observação</label>
            <input
              type="text"
              class="form-control"
              id="description"
              name="description"
              value="<%= appo.description %>"
            />
          </div>
          <div class="mb-3">
            <label for="date" class="form-label">Data</label>
            <input
              type="date"
              class="form-control"
              id="date"
              name="date"
              value="<%= new Date(appo.date).toISOString().split('T')[0] %>"
              required
            />
          </div>
          <div class="mb-3">
            <label for="time" class="form-label">Horário</label>
            <input
              type="time"
              class="form-control"
              id="time"
              name="time"
              value="<%= appo.time %>"
              required
            />
          </div>
          
          <div class="mb-3">
            <label for="email" class="form-label">Email</label>
            <input
              type="email"
              class="form-control"
              id="email"
              name="email"
              value="<%= appo.email %>"
            />
          </div>
          
          <button class="btn btn-primary w-100">Salvar Alterações</button>
        </form>
        <a href="/" class="btn btn-secondary w-100 mt-2">Cancelar</a>
      </div>
    </div>

  </body>
</html>
